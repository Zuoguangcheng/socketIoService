let info = require('./model');

/*class RoomCollection {
  constructor() {
    this.rooms = [];
    for (let i = 0; i < 3; i++) {
      let room = new info('room' + i, 'room' + i);
      this.rooms.push(room);
    }
  }
  getRooms() {
    return this.rooms;
  }
}*/

module.exports = info;